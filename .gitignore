# Live metrics — contains farm personas, handles, post URLs — keep local
farm_metrics.json
farm_history.json
upload_post_metrics.json
loudly_metrics.json
metrics.json
.DS_Store

# farm_metrics_public.json IS committed — it's the anonymized public version
# (no profile names, no handles, no post URLs — only aggregate numbers + timeseries)
!farm_metrics_public.json

# Royalty monitor.
# royalties.json IS published (user opted to make royalty data fully public).
# Raw scrape input + standalone claim files stay local.
royalties_raw.json
claims/
scraper/ditto_reports/
